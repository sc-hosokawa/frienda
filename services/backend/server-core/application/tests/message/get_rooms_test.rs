#[cfg(test)]
mod tests {
    use std::sync::Arc;

    #[tokio::test]
    async fn test_placeholder() {
        // TODO: Implement get_rooms test
        // This is a placeholder test to prevent compilation errors
        assert_eq!(1, 1);
    }
}
